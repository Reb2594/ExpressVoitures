
@model ExpressVoitures.Models.Entities.Vehicule
@{
    ViewData["Title"] = "Voiture supprimée";
}

<div class="container text-center mt-5">
    <h1>
        Votre @Model.Annee.Valeur @Model.Marque.Nom @Model.Modele.Nom @Model.Finition.Nom
    </h1>
    <p>a bien été supprimée !</p>
    <a href="@Url.Action("Index", "Vehicule", new { id = Model.Id })" class="btn btn-primary">Retour à l'accueil</a>
</div>
}
